# Ahead agent rules

Ahead 是一个快速迭代的 vibe coding 项目。优先交付清晰、直接、可删除的实现，不为假设中的未来需求增加结构。

- 先读相关代码与文档，再修改；以当前产品行为和 `packages/schema/schemas` 中的 OEF Schema 为准。
- 选择最简单的可行实现。不要预建抽象层、兼容层、迁移层、备用路径或配置项；只有当前真实数据和已确认需求需要时才加入。
- 不要吞掉异常。只在能恢复、能转成明确用户反馈，或离线缓存属于既定产品能力时捕获错误。
- 不为 UI、样式、组件、交互或实现细节编写单元测试、快照测试和 E2E 测试。
- 测试仅用于 OEF 协议与 Schema 的少量稳定契约；保持内联、短小，不新增 fixture、mock 数据目录或 golden 文件。
- 默认验证方式是 `pnpm typecheck`、`pnpm build` 和针对改动的人工检查。不要为了验证创建一次性脚本。
- 不新增示例目录、演示页面、调试入口或长篇说明文档，除非用户明确要求。协议示例直接放在协议文档中。
- 修改功能时同步删除旧分支、废弃文案、无用类型和不再使用的依赖；不要保留“也许以后有用”的代码。
- 用户界面面向普通用户：避免 demo 味、开发术语、冗余说明和内部错误细节。
- 保持现有 pnpm monorepo 边界；复用源码中的类型与能力，不复制协议定义或业务规则。
- 除非用户明确要求，否则不要进行视觉验收

